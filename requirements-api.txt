# Slim dependency set for the API web service only (no scrapers).
# The scheduler / pipelines use the full requirements.txt instead.
fastapi
uvicorn[standard]
psycopg[binary]
psycopg-pool
python-dotenv
requests
beautifulsoup4

# Live Activity pushes: direct APNs over HTTP/2 with an ES256 provider JWT.
PyJWT
cryptography
httpx[http2]
